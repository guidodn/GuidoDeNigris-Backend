class ProductManager {
    products;
    constructor() {
      this.products = []
    }
    getProducts () {
        return this.products;
    }
    addProduct (
        title,
        id,
        description,
        price,
        thumbnail,
        code,
        stock
    )
    
    if (productsid === productsId){
        products.cantidad++

    }else{
        const item = productos.find((products) => products.id === productsId)

        products.push(item)
    }
    
}

const agregarProductos = new AddProduct ()
agregarProductos.AddProduct ('Primer desafio', 1, 'Este es el primer desafio', 1500, 'noimage', 'code123', 25)

